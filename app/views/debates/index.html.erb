<% provide(:title, 'Home') %>

<br>

<%= will_paginate @debates, renderer: FoundationPagination::Rails %>

<%= render 'debate_table_list' %>

<%= will_paginate @debates, renderer: FoundationPagination::Rails %>

<br>

<%= link_to 'Submit a New Debate', new_debate_path %>
